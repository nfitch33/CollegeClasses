# Git's Internals

In the repository in a given `repo.tar.gz` file, what is the **type** and **size** of an object with the ID `109f1e9348535c45c83c2206a7d5aa594d8784d3`.

## Submission

Write your answers in `answer.txt`. On the first line writes the object's type. On the next
line writes the object's size.

## Note

This problem will be manually graded.
