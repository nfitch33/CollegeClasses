### function `is_one` and `is_three`

Implement an is_one function that return True for Succ Zero and False for anything else.

### function `add_the_last_element` with a list as the parameter
Implement function add_the_last_element so that element is added to the end of lst.

let add_the_last_element lst element = ...

### function `double_elements` to double all elements in a list
```ocaml
let double x = 2 * x
let square x = x * x
```
### Use `fold` to implement the `reverse` function for lists


### function max_even - find out the maximum even number in a list
* Bonus: use `fold`

### function count_pos - count the number of positive numbers in a list
* Bonus: use `fold`

---

* Functional data structure (ch5.6)
* Options vs exceptions (ch5.6.3) (ch3.7 Options)

### functions `first_10_primes` and `first_10_odd_numbers`

```
let first_10_primes = take 10 primes
let first_10_odd_numbers = take 10 odd_numbers
let first_10_nats = take 10 nats

```
