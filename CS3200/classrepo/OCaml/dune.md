# dune commands
```console
dune build --help
```


### dune output
To see the success output before it disappears, run the following. 
`dune build --watch` or `dune build -w`
`dune test -w`.
Use `^c` to go back to command prompt.
